<% provide(:title, "Log in") %>
<h1>Đăng nhập</h1>
<div class="row" data-controller="password-toggle">
	<div class="col-md-6 col-md-offset-3">
		<%= form_with(url: login_path, scope: :session, local: true) do |f| %>
			<%= f.label :ID, "Mã số" %>
			<%= f.number_field :accountname, class: 'form-control', placeholder: "Mã số", type: "number" %>
			<%= f.label :password, "Mật khẩu" %>
			<%= f.password_field :password, class: 'form-control', placeholder: "Mật khẩu" %>
			<%= f.submit "Đăng nhập", class: "btn btn-primary" %>
		<% end %>
	</div>
</div>

